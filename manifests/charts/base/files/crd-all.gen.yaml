# DO NOT EDIT - Generated by Cue OpenAPI generator based on Dubbo APIs.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    "helm.sh/resource-policy": keep
  labels:
    app: dubbo
    chart: dubbo
    dubbo: networking
    heritage: Tiller
    release: dubbo
  name: circuitbreakerpolicies.networking.dubbo.apache.org
spec:
  group: networking.dubbo.apache.org
  names:
    categories:
    - dubbo
    - networking
    kind: CircuitBreakerPolicy
    listKind: CircuitBreakerPolicyList
    plural: circuitbreakerpolicies
    shortNames:
    - cbp
    singular: circuitbreakerpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Gateway API targets.
      jsonPath: .spec.targetRefs[*].name
      name: Targets
      type: string
    - description: CreationTimestamp is a timestamp representing the server time when
        this object was created.
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha3
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: 'Gateway API policy attachment for backend circuit breaking.
              See more details at: '
            properties:
              connectionPool:
                description: Connection pool limits.
                properties:
                  http1MaxPendingRequests:
                    description: Maximum queued requests waiting for an upstream connection.
                    format: int32
                    type: integer
                  http2MaxRequests:
                    description: Maximum concurrent requests for HTTP/2 or gRPC upstreams.
                    format: int32
                    type: integer
                  maxConnections:
                    description: Maximum concurrent upstream connections.
                    format: int32
                    type: integer
                  maxRequestsPerConnection:
                    description: Maximum requests before a connection is drained.
                    format: int32
                    type: integer
                  maxRetries:
                    description: Maximum retries allowed to the backend.
                    format: int32
                    type: integer
                type: object
              outlierDetection:
                description: Outlier detection controls passive ejection of failing
                  endpoints.
                properties:
                  baseEjectionTime:
                    description: Minimum ejection time for an unhealthy endpoint.
                    type: string
                    x-kubernetes-validations:
                    - message: must be a valid duration greater than 1ms
                      rule: duration(self) >= duration('1ms')
                  consecutive5xxErrors:
                    description: Number of consecutive 5xx responses before an endpoint
                      is considered an outlier.
                    maximum: 4294967295
                    minimum: 0
                    nullable: true
                    type: integer
                  interval:
                    description: Time between outlier detection sweeps.
                    type: string
                    x-kubernetes-validations:
                    - message: must be a valid duration greater than 1ms
                      rule: duration(self) >= duration('1ms')
                  maxEjectionPercent:
                    description: Maximum percentage of endpoints that can be ejected.
                    format: int32
                    type: integer
                  minHealthPercent:
                    description: Minimum healthy percentage before outlier detection
                      is disabled.
                    format: int32
                    type: integer
                type: object
              targetRefs:
                description: Gateway API policy targets.
                items:
                  properties:
                    group:
                      description: API group of the target.
                      type: string
                    kind:
                      description: Kind of the target.
                      type: string
                    name:
                      description: Name of the target object.
                      type: string
                    sectionName:
                      description: Optional section name for future per-port attachment.
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                type: array
            required:
            - targetRefs
            type: object
          status:
            properties:
              conditions:
                items:
                  properties:
                    observedGeneration:
                      anyOf:
                      - type: integer
                      - type: string
                      x-kubernetes-int-or-string: true
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  type: object
                type: array
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    "helm.sh/resource-policy": keep
  labels:
    app: dubbo
    chart: dubbo
    dubbo: networking
    heritage: Tiller
    release: dubbo
  name: serviceentries.networking.dubbo.apache.org
spec:
  group: networking.dubbo.apache.org
  names:
    categories:
    - dubbo
    - networking
    kind: ServiceEntry
    listKind: ServiceEntryList
    plural: serviceentries
    shortNames:
    - se
    singular: serviceentry
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Service hosts.
      jsonPath: .spec.hosts
      name: Hosts
      type: string
    - description: Service location.
      jsonPath: .spec.location
      name: Location
      type: string
    - description: Service resolution.
      jsonPath: .spec.resolution
      name: Resolution
      type: string
    - description: CreationTimestamp is a timestamp representing the server time when
        this object was created.
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha3
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: 'Declarative entries for the internal service registry. See
              more details at: '
            properties:
              addresses:
                description: Virtual IP addresses associated with the service.
                items:
                  type: string
                maxItems: 256
                type: array
              endpoints:
                description: Inline endpoints associated with the service.
                items:
                  properties:
                    address:
                      description: IP address or fully-qualified DNS name of the workload.
                      maxLength: 256
                      minLength: 1
                      type: string
                    labels:
                      additionalProperties:
                        type: string
                      description: Labels used by ServiceEntry.workloadSelector.
                      maxProperties: 256
                      type: object
                    locality:
                      description: Locality in region/zone/subzone form.
                      maxLength: 2048
                      type: string
                    network:
                      description: Network containing the workload.
                      maxLength: 2048
                      type: string
                    ports:
                      additionalProperties:
                        maximum: 4294967295
                        minimum: 0
                        type: integer
                        x-kubernetes-validations:
                        - message: port must be between 1-65535
                          rule: 0 < self && self <= 65535
                      description: Per-service-port endpoint ports.
                      maxProperties: 128
                      type: object
                    serviceAccount:
                      description: Service account used to derive workload identity.
                      maxLength: 253
                      type: string
                    weight:
                      description: Relative load-balancing weight.
                      maximum: 4294967295
                      minimum: 0
                      type: integer
                  type: object
                maxItems: 4096
                type: array
              exportTo:
                description: Namespaces to which the service is exported.
                items:
                  type: string
                type: array
              hosts:
                description: DNS hosts associated with the service.
                items:
                  type: string
                maxItems: 256
                minItems: 1
                type: array
              location:
                description: |2-


                  Valid Options: MESH_EXTERNAL, MESH_INTERNAL
                enum:
                - MESH_EXTERNAL
                - MESH_INTERNAL
                type: string
              ports:
                description: Ports exposed by the service.
                items:
                  properties:
                    name:
                      description: Stable name used by WorkloadEntry.ports.
                      maxLength: 256
                      type: string
                    number:
                      maximum: 4294967295
                      minimum: 0
                      type: integer
                      x-kubernetes-validations:
                      - message: port must be between 1-65535
                        rule: 0 < self && self <= 65535
                    protocol:
                      description: Protocol exposed by the port, for example GRPC,
                        HTTP2, TCP, or TLS.
                      maxLength: 256
                      type: string
                    targetPort:
                      description: Default endpoint port.
                      maximum: 4294967295
                      minimum: 0
                      type: integer
                  required:
                  - number
                  - name
                  type: object
                maxItems: 256
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              resolution:
                description: |2-


                  Valid Options: NONE, STATIC, DNS, DNS_ROUND_ROBIN
                enum:
                - NONE
                - STATIC
                - DNS
                - DNS_ROUND_ROBIN
                type: string
              subjectAltNames:
                description: Subject alternative names accepted for the service.
                items:
                  type: string
                type: array
              workloadSelector:
                description: Selects WorkloadEntry resources in the same namespace.
                properties:
                  matchLabels:
                    additionalProperties:
                      maxLength: 63
                      type: string
                      x-kubernetes-validations:
                      - message: wildcard not allowed in label value match
                        rule: '!self.contains("*")'
                    maxProperties: 4096
                    type: object
                    x-kubernetes-validations:
                    - message: wildcard not allowed in label key match
                      rule: self.all(key, !key.contains("*"))
                    - message: key must not be empty
                      rule: self.all(key, key.size() != 0)
                type: object
            required:
            - hosts
            type: object
            x-kubernetes-validations:
            - message: only one of workloadSelector or endpoints can be set
              rule: '!(has(self.workloadSelector) && has(self.endpoints))'
          status:
            properties:
              conditions:
                items:
                  properties:
                    observedGeneration:
                      anyOf:
                      - type: integer
                      - type: string
                      x-kubernetes-int-or-string: true
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  type: object
                type: array
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    "helm.sh/resource-policy": keep
  labels:
    app: dubbo
    chart: dubbo
    dubbo: networking
    heritage: Tiller
    release: dubbo
  name: workloadentries.networking.dubbo.apache.org
spec:
  group: networking.dubbo.apache.org
  names:
    categories:
    - dubbo
    - networking
    kind: WorkloadEntry
    listKind: WorkloadEntryList
    plural: workloadentries
    shortNames:
    - we
    singular: workloadentry
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Workload address.
      jsonPath: .spec.address
      name: Address
      type: string
    - description: CreationTimestamp is a timestamp representing the server time when
        this object was created.
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha3
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: 'Configuration describing a non-Kubernetes workload. See
              more details at: '
            properties:
              address:
                description: IP address or fully-qualified DNS name of the workload.
                maxLength: 256
                minLength: 1
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Labels used by ServiceEntry.workloadSelector.
                maxProperties: 256
                type: object
              locality:
                description: Locality in region/zone/subzone form.
                maxLength: 2048
                type: string
              network:
                description: Network containing the workload.
                maxLength: 2048
                type: string
              ports:
                additionalProperties:
                  maximum: 4294967295
                  minimum: 0
                  type: integer
                  x-kubernetes-validations:
                  - message: port must be between 1-65535
                    rule: 0 < self && self <= 65535
                description: Per-service-port endpoint ports.
                maxProperties: 128
                type: object
              serviceAccount:
                description: Service account used to derive workload identity.
                maxLength: 253
                type: string
              weight:
                description: Relative load-balancing weight.
                maximum: 4294967295
                minimum: 0
                type: integer
            type: object
          status:
            properties:
              conditions:
                items:
                  properties:
                    observedGeneration:
                      anyOf:
                      - type: integer
                      - type: string
                      x-kubernetes-int-or-string: true
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  type: object
                type: array
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    "helm.sh/resource-policy": keep
  labels:
    app: dubbo
    chart: dubbo
    dubbo: security
    heritage: Tiller
    release: dubbo
  name: authorizationpolicys.security.dubbo.apache.org
spec:
  group: security.dubbo.apache.org
  names:
    categories:
    - dubbo
    - security
    kind: AuthorizationPolicy
    listKind: AuthorizationPolicyList
    plural: authorizationpolicys
    shortNames:
    - ap
    singular: authorizationpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The authorization action.
      jsonPath: .spec.action
      name: Action
      type: string
    - description: 'CreationTimestamp is a timestamp representing the server time
        when this object was created. It is not guaranteed to be set in happens-before
        order across separate operations. Clients may not set this value. It is represented
        in RFC3339 form and is in UTC. Populated by the system. Read-only. Null for
        lists. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#metadata'
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha3
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: 'Authorization policy configuration for workloads. See more
              details at: '
            properties:
              action:
                description: |-
                  The action to take if a request matches the rules.

                  Valid Options: ALLOW, DENY
                enum:
                - ALLOW
                - DENY
                type: string
              rules:
                description: Authorization rules.
                items:
                  properties:
                    from:
                      description: Sources of a request.
                      items:
                        properties:
                          source:
                            description: Source attributes.
                            properties:
                              requestPrincipals:
                                description: Request identities derived from validated
                                  JWT issuer and subject.
                                items:
                                  type: string
                                type: array
                            type: object
                        type: object
                      type: array
                    when:
                      description: Additional conditions that must match.
                      items:
                        properties:
                          key:
                            description: Attribute key.
                            type: string
                          notValues:
                            description: Disallowed values.
                            items:
                              type: string
                            type: array
                          values:
                            description: Allowed values.
                            items:
                              type: string
                            type: array
                        type: object
                      type: array
                  type: object
                type: array
              selector:
                description: The selector determines the workloads to apply the authorization
                  policy on.
                properties:
                  matchLabels:
                    additionalProperties:
                      maxLength: 63
                      type: string
                      x-kubernetes-validations:
                      - message: wildcard not allowed in label value match
                        rule: '!self.contains("*")'
                    maxProperties: 4096
                    type: object
                    x-kubernetes-validations:
                    - message: wildcard not allowed in label key match
                      rule: self.all(key, !key.contains("*"))
                    - message: key must not be empty
                      rule: self.all(key, key.size() != 0)
                type: object
            type: object
          status:
            properties:
              conditions:
                items:
                  properties:
                    observedGeneration:
                      anyOf:
                      - type: integer
                      - type: string
                      x-kubernetes-int-or-string: true
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  type: object
                type: array
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    "helm.sh/resource-policy": keep
  labels:
    app: dubbo
    chart: dubbo
    dubbo: security
    heritage: Tiller
    release: dubbo
  name: peerauthentications.security.dubbo.apache.org
spec:
  group: security.dubbo.apache.org
  names:
    categories:
    - dubbo
    - security
    kind: PeerAuthentication
    listKind: PeerAuthenticationList
    plural: peerauthentications
    shortNames:
    - pa
    singular: peerauthentication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Defines the mTLS mode used for peer authentication.
      jsonPath: .spec.mtls.mode
      name: Mode
      type: string
    - description: 'CreationTimestamp is a timestamp representing the server time
        when this object was created. It is not guaranteed to be set in happens-before
        order across separate operations. Clients may not set this value. It is represented
        in RFC3339 form and is in UTC. Populated by the system. Read-only. Null for
        lists. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#metadata'
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha3
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: 'Peer authentication configuration for workloads. See more
              details at: '
            properties:
              mtls:
                description: Mutual TLS settings for workload.
                properties:
                  mode:
                    description: |-
                      Defines the mTLS mode used for peer authentication.

                      Valid Options: DISABLE, PERMISSIVE, STRICT
                    enum:
                    - UNSET
                    - DISABLE
                    - PERMISSIVE
                    - STRICT
                    type: string
                type: object
              portLevelMtls:
                additionalProperties:
                  properties:
                    mode:
                      description: |-
                        Defines the mTLS mode used for peer authentication.

                        Valid Options: DISABLE, PERMISSIVE, STRICT
                      enum:
                      - UNSET
                      - DISABLE
                      - PERMISSIVE
                      - STRICT
                      type: string
                  type: object
                description: Port specific mutual TLS settings.
                minProperties: 1
                type: object
                x-kubernetes-validations:
                - message: port must be between 1-65535
                  rule: self.all(key, 0 < int(key) && int(key) <= 65535)
              selector:
                description: The selector determines the workloads to apply the PeerAuthentication
                  on.
                properties:
                  matchLabels:
                    additionalProperties:
                      maxLength: 63
                      type: string
                      x-kubernetes-validations:
                      - message: wildcard not allowed in label value match
                        rule: '!self.contains("*")'
                    maxProperties: 4096
                    type: object
                    x-kubernetes-validations:
                    - message: wildcard not allowed in label key match
                      rule: self.all(key, !key.contains("*"))
                    - message: key must not be empty
                      rule: self.all(key, key.size() != 0)
                type: object
            type: object
            x-kubernetes-validations:
            - message: portLevelMtls requires selector
              rule: 'has(self.portLevelMtls) ? (((has(self.selector) && has(self.selector.matchLabels))
                ? self.selector.matchLabels : {}).size() > 0) : true'
          status:
            properties:
              conditions:
                items:
                  properties:
                    observedGeneration:
                      anyOf:
                      - type: integer
                      - type: string
                      x-kubernetes-int-or-string: true
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  type: object
                type: array
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    "helm.sh/resource-policy": keep
  labels:
    app: dubbo
    chart: dubbo
    dubbo: security
    heritage: Tiller
    release: dubbo
  name: requestauthentications.security.dubbo.apache.org
spec:
  group: security.dubbo.apache.org
  names:
    categories:
    - dubbo
    - security
    kind: RequestAuthentication
    listKind: RequestAuthenticationList
    plural: requestauthentications
    shortNames:
    - ra
    singular: requestauthentication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: JWT issuers accepted by this policy.
      jsonPath: .spec.jwtRules[*].issuer
      name: Issuer
      type: string
    - description: 'CreationTimestamp is a timestamp representing the server time
        when this object was created. It is not guaranteed to be set in happens-before
        order across separate operations. Clients may not set this value. It is represented
        in RFC3339 form and is in UTC. Populated by the system. Read-only. Null for
        lists. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#metadata'
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha3
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: 'Request authentication configuration for workloads. See
              more details at: '
            properties:
              jwtRules:
                description: Rules to validate JWT tokens found in the request.
                items:
                  properties:
                    audiences:
                      description: Audiences accepted by this rule.
                      items:
                        type: string
                      type: array
                    fromHeaders:
                      description: JWT locations in request headers.
                      items:
                        properties:
                          name:
                            description: Header name.
                            type: string
                          prefix:
                            description: Optional prefix stripped before JWT validation.
                            type: string
                        type: object
                      type: array
                    fromParams:
                      description: JWT locations in query parameters.
                      items:
                        type: string
                      type: array
                    issuer:
                      description: Identifies the issuer that issued the JWT.
                      type: string
                    jwks:
                      description: Inline JSON Web Key Set.
                      type: string
                    jwksUri:
                      description: URL of the JSON Web Key Set used to validate tokens.
                      type: string
                  type: object
                type: array
              selector:
                description: The selector determines the workloads to apply the request
                  authentication on.
                properties:
                  matchLabels:
                    additionalProperties:
                      maxLength: 63
                      type: string
                      x-kubernetes-validations:
                      - message: wildcard not allowed in label value match
                        rule: '!self.contains("*")'
                    maxProperties: 4096
                    type: object
                    x-kubernetes-validations:
                    - message: wildcard not allowed in label key match
                      rule: self.all(key, !key.contains("*"))
                    - message: key must not be empty
                      rule: self.all(key, key.size() != 0)
                type: object
            type: object
          status:
            properties:
              conditions:
                items:
                  properties:
                    observedGeneration:
                      anyOf:
                      - type: integer
                      - type: string
                      x-kubernetes-int-or-string: true
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  type: object
                type: array
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    "helm.sh/resource-policy": keep
  labels:
    app: dubbo
    chart: dubbo
    dubbo: telemetry
    heritage: Tiller
    release: dubbo
  name: telemetries.telemetry.dubbo.apache.org
spec:
  group: telemetry.dubbo.apache.org
  names:
    categories:
    - dubbo
    - telemetry
    kind: Telemetry
    listKind: TelemetryList
    plural: telemetries
    shortNames:
    - telemetry
    singular: telemetry
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: 'CreationTimestamp is a timestamp representing the server time
        when this object was created. It is not guaranteed to be set in happens-before
        order across separate operations. Clients may not set this value. It is represented
        in RFC3339 form and is in UTC. Populated by the system. Read-only. Null for
        lists. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#metadata'
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: 'Telemetry configuration for workloads. See more details
              at: '
            properties:
              selector:
                description: The selector determines the workloads to apply the Telemetry
                  on.
                properties:
                  matchLabels:
                    additionalProperties:
                      maxLength: 63
                      type: string
                      x-kubernetes-validations:
                      - message: wildcard not allowed in label value match
                        rule: '!self.contains("*")'
                    maxProperties: 4096
                    type: object
                    x-kubernetes-validations:
                    - message: wildcard not allowed in label key match
                      rule: self.all(key, !key.contains("*"))
                    - message: key must not be empty
                      rule: self.all(key, key.size() != 0)
                type: object
              tracing:
                description: Tracing configures the tracing behavior for all selected
                  workloads.
                items:
                  properties:
                    disableSpanReporting:
                      description: Controls span reporting.
                      nullable: true
                      type: boolean
                    providers:
                      description: Providers used for span reporting.
                      items:
                        properties:
                          name:
                            description: REQUIRED.
                            type: string
                        type: object
                      type: array
                    randomSamplingPercentage:
                      description: Controls the rate at which traffic will be selected
                        for tracing if no prior sampling decision has been made.
                      format: double
                      nullable: true
                      type: number
                    tags:
                      description: Static span tags.
                      items:
                        properties:
                          name:
                            description: REQUIRED.
                            type: string
                          value:
                            description: Span attribute value.
                            type: string
                        type: object
                      type: array
                  type: object
                type: array
            type: object
          status:
            properties:
              conditions:
                items:
                  properties:
                    observedGeneration:
                      anyOf:
                      - type: integer
                      - type: string
                      x-kubernetes-int-or-string: true
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  type: object
                type: array
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
