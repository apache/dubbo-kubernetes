# DO NOT EDIT - Generated by Cue OpenAPI generator based on Dubbo APIs.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    "helm.sh/resource-policy": keep
  labels:
    app: dubbo
    chart: dubbo
    heritage: Tiller
    release: dubbo
  name: meshservices.networking.dubbo.apache.org
spec:
  group: networking.dubbo.apache.org
  names:
    categories:
    - dubbo
    - networking
    kind: MeshService
    listKind: MeshServiceList
    plural: meshservices
    shortNames:
    - ms
    singular: meshservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The destination hosts to which service traffic is being sent
      jsonPath: .spec.hosts
      name: Hosts
      type: string
    - description: 'CreationTimestamp is a timestamp representing the server time
        when this object was created. It is not guaranteed to be set in happens-before
        order across separate operations. Clients may not set this value. It is represented
        in RFC3339 form and is in UTC. Populated by the system. Read-only. Null for
        lists. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#metadata'
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha3
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: 'Service-to-service routing and traffic policy. See more
              details at: '
            properties:
              hosts:
                description: Hosts addressed by this service-to-service policy.
                items:
                  type: string
                type: array
              routes:
                description: Ordered route entries.
                items:
                  properties:
                    service:
                      description: One route entry can split traffic across multiple
                        services.
                      items:
                        properties:
                          host:
                            type: string
                          labels:
                            additionalProperties:
                              type: string
                            type: object
                          name:
                            type: string
                          port:
                            properties:
                              name:
                                type: string
                              number:
                                maximum: 4294967295
                                minimum: 0
                                type: integer
                            type: object
                          trafficPolicy:
                            properties:
                              tls:
                                description: TLS related settings for connections
                                  to the upstream service.
                                properties:
                                  mode:
                                    description: |-
                                      Indicates whether connections to this port should be secured using TLS.

                                      Valid Options: DISABLE, MUTUAL, DUBBO_MUTUAL
                                    type: string
                                    x-kubernetes-int-or-string: true
                                type: object
                            type: object
                          weight:
                            format: int32
                            type: integer
                        required:
                        - host
                        type: object
                      type: array
                  type: object
                type: array
              rules:
                description: Match rules for selecting traffic.
                properties:
                  match:
                    description: Multiple match entries are ORed; fields inside one
                      match entry are ANDed.
                    items:
                      properties:
                        headers:
                          additionalProperties:
                            oneOf:
                            - not:
                                anyOf:
                                - required:
                                  - exact
                                - required:
                                  - prefix
                                - required:
                                  - regex
                            - required:
                              - exact
                            - required:
                              - prefix
                            - required:
                              - regex
                            properties:
                              exact:
                                type: string
                              prefix:
                                type: string
                              regex:
                                type: string
                            type: object
                          type: object
                        method:
                          oneOf:
                          - not:
                              anyOf:
                              - required:
                                - exact
                              - required:
                                - prefix
                              - required:
                                - regex
                          - required:
                            - exact
                          - required:
                            - prefix
                          - required:
                            - regex
                          properties:
                            exact:
                              type: string
                            prefix:
                              type: string
                            regex:
                              type: string
                          type: object
                        port:
                          maximum: 4294967295
                          minimum: 0
                          type: integer
                        queryParams:
                          additionalProperties:
                            oneOf:
                            - not:
                                anyOf:
                                - required:
                                  - exact
                                - required:
                                  - prefix
                                - required:
                                  - regex
                            - required:
                              - exact
                            - required:
                              - prefix
                            - required:
                              - regex
                            properties:
                              exact:
                                type: string
                              prefix:
                                type: string
                              regex:
                                type: string
                            type: object
                          type: object
                        uri:
                          oneOf:
                          - not:
                              anyOf:
                              - required:
                                - exact
                              - required:
                                - prefix
                              - required:
                                - regex
                          - required:
                            - exact
                          - required:
                            - prefix
                          - required:
                            - regex
                          properties:
                            exact:
                              type: string
                            prefix:
                              type: string
                            regex:
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              trafficPolicy:
                description: Default traffic policy for every destination in this
                  MeshService.
                properties:
                  tls:
                    description: TLS related settings for connections to the upstream
                      service.
                    properties:
                      mode:
                        description: |-
                          Indicates whether connections to this port should be secured using TLS.

                          Valid Options: DISABLE, MUTUAL, DUBBO_MUTUAL
                        type: string
                        x-kubernetes-int-or-string: true
                    type: object
                type: object
              visibleTo:
                description: Namespaces that can see this mesh service.
                items:
                  type: string
                type: array
            type: object
          status:
            properties:
              conditions:
                items:
                  properties:
                    observedGeneration:
                      anyOf:
                      - type: integer
                      - type: string
                      x-kubernetes-int-or-string: true
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  type: object
                type: array
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    "helm.sh/resource-policy": keep
  labels:
    app: dubbo
    chart: dubbo
    dubbo: security
    heritage: Tiller
    release: dubbo
  name: peerauthentications.security.dubbo.apache.org
spec:
  group: security.dubbo.apache.org
  names:
    categories:
    - dubbo
    - security
    kind: PeerAuthentication
    listKind: PeerAuthenticationList
    plural: peerauthentications
    shortNames:
    - pa
    singular: peerauthentication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Defines the mTLS mode used for peer authentication.
      jsonPath: .spec.mtls.mode
      name: Mode
      type: string
    - description: 'CreationTimestamp is a timestamp representing the server time
        when this object was created. It is not guaranteed to be set in happens-before
        order across separate operations. Clients may not set this value. It is represented
        in RFC3339 form and is in UTC. Populated by the system. Read-only. Null for
        lists. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#metadata'
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha3
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: 'Peer authentication configuration for workloads. See more
              details at: '
            properties:
              mtls:
                description: Mutual TLS settings for workload.
                properties:
                  mode:
                    description: |-
                      Defines the mTLS mode used for peer authentication.

                      Valid Options: DISABLE, PERMISSIVE, STRICT
                    type: string
                    x-kubernetes-int-or-string: true
                type: object
              portLevelMtls:
                additionalProperties:
                  properties:
                    mode:
                      description: |-
                        Defines the mTLS mode used for peer authentication.

                        Valid Options: DISABLE, PERMISSIVE, STRICT
                      type: string
                      x-kubernetes-int-or-string: true
                  type: object
                description: Port specific mutual TLS settings.
                minProperties: 1
                type: object
                x-kubernetes-validations:
                - message: port must be between 1-65535
                  rule: self.all(key, 0 < int(key) && int(key) <= 65535)
              selector:
                description: The selector determines the workloads to apply the PeerAuthentication
                  on.
                properties:
                  matchLabels:
                    additionalProperties:
                      maxLength: 63
                      type: string
                      x-kubernetes-validations:
                      - message: wildcard not allowed in label value match
                        rule: '!self.contains("*")'
                    maxProperties: 4096
                    type: object
                    x-kubernetes-validations:
                    - message: wildcard not allowed in label key match
                      rule: self.all(key, !key.contains("*"))
                    - message: key must not be empty
                      rule: self.all(key, key.size() != 0)
                type: object
            type: object
            x-kubernetes-validations:
            - message: portLevelMtls requires selector
              rule: 'has(self.portLevelMtls) ? (((has(self.selector) && has(self.selector.matchLabels))
                ? self.selector.matchLabels : {}).size() > 0) : true'
          status:
            properties:
              conditions:
                items:
                  properties:
                    observedGeneration:
                      anyOf:
                      - type: integer
                      - type: string
                      x-kubernetes-int-or-string: true
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  type: object
                type: array
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
